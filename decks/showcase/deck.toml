category = "Reference"
order = 0
quality = "medium_quality"
slug = "showcase"
summary = "Demonstrates every helper Simplex adds on top of vanilla Manim."
tags = ["reference", "showcase"]
theme = "simplex_dark"
title = "Simplex showcase"
voiceover = false

[[entrypoints]]
target = "slides.scenes:TextHelpers"

[[entrypoints]]
target = "slides.scenes:CodeHelpers"

[[entrypoints]]
target = "slides.scenes:CodeWithMath"

[[entrypoints]]
target = "slides.scenes:GraphAndArray"

[[entrypoints]]
target = "slides.scenes:RegionAnchors"

[[entrypoints]]
target = "slides.scenes:OutlineHelpers"

[[entrypoints]]
target = "slides.scenes:ExitAnimations"

[[entrypoints]]
target = "slides.scenes:GeometryHelpers"

[[entrypoints]]
target = "slides.scenes:GlyphMapTransform"

[[entrypoints]]
target = "slides.scenes:TrackingHelpers"

[[entrypoints]]
target = "slides.scenes:ShapeAndDebug"

[[entrypoints]]
target = "slides.scenes:ScalingHelpers"

[[entrypoints]]
target = "slides.paper_showcase:PaperShowcase"

[[entrypoints]]
renderer = "opengl"
target = "slides.surface_showcase:SurfaceColoring"

# Slide chrome lives in the RevealJS host, not the rendered frames.
# Toggling these on costs nothing -- no re-render needed.
[web]
show_clock = true
show_slide_number = true
